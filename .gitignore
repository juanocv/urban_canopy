# Secrets
.env
.env.*
!.env.example

# Generated artifacts
artifacts_out/
# Figures, tables and docs generated by notebooks/03_report_figures.ipynb.
report_out/
report_sbc/

debug_out/
/*.png
/*.jpg
/*.jpeg
/*.json
/*.csv
# Study configuration is an input, not an output, and belongs in the commit that
# reports the numbers it produced. Without this the taxonomy file that --taxonomy
# reads would be ignored at the repo root with no sign that it was.
!/taxonomy*.json

# Python cache/test output
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
*.egg-info/
build/
dist/

# Environments
venv/
.venv/

# Local datasets and model checkpoints
data/
checkpoints/
urban_canopy/models/checkpoints/

# Third-party model checkouts kept outside the package build
DeepLabV3Plus-Pytorch/
DeepLabV3Plus-network/
detectron2/
OneFormer/

# Jupyter
.ipynb_checkpoints/
